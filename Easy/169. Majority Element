class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        # Since the element should appear more than n/2 times
        # we just sort the array and return the middle element ezy pzy
        nums.sort()
        return nums[int(len(nums)/2)]
